terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }

  required_version = ">= 1.2.0"
}

provider "aws" {
  access_key = AWS_ACCESS_KEY
  secret_key = AWS_SECRET_KEY
  region = AWS_REGION
}
